Public Class frmEntry1

    Public Sub New()
        ConstructorHelper(-1)
    End Sub

    Public Sub New(id As Integer)
        ConstructorHelper(id)
    End Sub

    Private Sub ConstructorHelper(id As Integer)
        ' This call is required by the designer.
        InitializeComponent()
        ' Add any initialization after the InitializeComponent() call.

    End Sub

End Class